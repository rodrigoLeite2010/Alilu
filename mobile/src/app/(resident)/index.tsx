import { PlaceholderScreen } from '../../components';

export default function ResidentHomeScreen() {
  return (
    <PlaceholderScreen
      title="Área do morador"
      subtitle="ResidentStack — telas ainda não implementadas."
    />
  );
}
