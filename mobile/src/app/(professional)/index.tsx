import { PlaceholderScreen } from '../../components';

export default function ProfessionalHomeScreen() {
  return (
    <PlaceholderScreen
      title="Área do profissional"
      subtitle="ProfessionalStack — telas ainda não implementadas."
    />
  );
}
