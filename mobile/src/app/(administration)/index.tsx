import { PlaceholderScreen } from '../../components';

export default function AdministrationHomeScreen() {
  return (
    <PlaceholderScreen
      title="Administração"
      subtitle="AdministrationStack — telas ainda não implementadas."
    />
  );
}
