# hooks/

Hooks compartilhados entre módulos (ex.: `useDebounce`, `useAppState`)
ficam aqui. Hooks específicos de um domínio (ex.: `useAgendamentos`) devem
ficar dentro do módulo correspondente em `src/modules/*`.

Nenhum hook foi criado ainda nesta etapa — pasta reservada conforme
PROMPT 02.
